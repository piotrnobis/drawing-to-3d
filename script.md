 ---
  Ortograph — "How it works" (≈60s)

  [0:00–0:08] — Hook / the page (Home)

  ▎ "Here's Ortograph. You hand it a flat engineering drawing — and it hands back a real, editable CAD solid you can trust. Not a
  ▎ mesh. A parametric STEP file."

  (Scroll into the hero: drawing → reconstruct → solid.)

  [0:08–0:20] — The loop (overview diagram)

  ▎ "Behind it, one AI conversation runs the whole loop: it reads the drawing, writes real CADQuery code, runs it in a sandbox, and
  ▎ renders it — then refines until it's right."

  (Pan across the overview diagram: analyze → generate → run → render → verify.)

  [0:20–0:38] — The idea that wins (three-signal verification)

  ▎ "Generating isn't the hard part — proving it's right is. So nothing ships until three independent checks agree.
  ▎ One: a vision model checks the shape matches the drawing — the right features, in the right places. Two: an
  ▎ independent judge — a separate model with no memory of how the part was built — reviews it fresh, and catches
  ▎ what the first model talked itself into. Three: a dimension gate measures every callout against the real geometry —
  ▎ exact numbers, not a guess. The eye and the judge cover the shape; the gate proves the size."

  (On "one / two / three", highlight each signal card in turn; then the dimension table ticks down PASS · PASS · PASS.)

  [0:38–0:46] — Staged build (the showcase)

  ▎ "For complex parts, it builds the way an engineer would — one verified feature at a time. Base, pillar, fillets, holes —
  ▎ each one locked before the next."

  (Sweep the 11-stage build grid, ending on the finished bracket.)

  [0:46–0:58] — The demo (click through)

  ▎ "And here's what a user actually does."

  (Click Demo. Pick a sample drawing. Click Reconstruct.)

  ▎ "Pick a drawing, hit reconstruct, and watch it assemble — then it's a live 3D model, every dimension measured and
  ▎ passing. Want one last change? Just describe it in plain language — and it edits the part before you export the STEP."

  (Stages animate → 3D viewer spins → gate table shows PASS → type a final prompt, the model updates → click Download .step.)

  [0:58–1:00] — Close

  ▎ "Ortograph. A drawing in — a solid you can trust, and edit, out."

  (Logo lockup: ORTOGRAPH — where blueprints become breakthroughs.)

  ---
  Timing & delivery notes:
  - It's now ~185 words (the three-signal beat and the new "final tweak" line both carry weight) — a hair long for 60s, so
  deliver at a steady clip. The easiest cut is the staged-build section (0:38–0:46): keep the visual sweep, trim the words.
  - The "final tweak" line teases the prompt-to-edit step — time it so you're typing the prompt as you say "describe it," and
  the model updates before "export the STEP." (UI/backend for this is still being wired; record it once it's live on the demo page.)
  - The three-signal beat is the heart of the pitch — number the signals out loud ("one… two… three…") so each lands distinctly,
  and pause a half-beat before "only when all three pass." If tight, drop the closing "the eye and the judge cover the shape…" line.
  - Pre-load the demo on the bracket run for the staged animation (best "watch it build" moment), or switch to the manifold if
  you want the cleaner "verified first try" story.
  - Land the final line exactly as the STEP download fires — it mirrors the headline and closes the loop.
  - Keep the cursor deliberate during the demo; the stage animation and the gate table filling in are the visual proof points —
  let them breathe for a beat each.

  One caveat: I've written this to match the UI as planned (Home with the three-signal + staged-build sections, and the Demo
  page with the run switcher, stage animation, 3D viewer, gate table, and download). If the live build differs slightly when you
  record, adjust the on-screen cues to whatever's actually on the page.